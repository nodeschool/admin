# General Settings

* --path or -p ......... to specify the path in which the operations should be run
   (defaults to .)
* --loglevel or -l ..... to change the loglevel. Supported options are
   silly/verbose/info/http/warn/error/silent
